var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/estimation/#Irregular-sampling-and-windowed-estimation","page":"Irregular & windowed","text":"Two estimators for data that an FFT can't handle directly: the Lomb–Scargle periodogram for irregularly-sampled series, and multitaper (DPSS / Slepian) averaging for low-variance spectra of short records.","title":"Irregular sampling & windowed estimation"},{"category":"section","location":"examples/estimation/#Lomb–Scargle-(irregular-sampling)","page":"Irregular & windowed","text":"Moorings, drifters, and satellite tracks sample at uneven times. lomb_scargle estimates the spectrum directly from (t, y) at a chosen set of (strictly positive) frequencies — no interpolation onto a regular grid.\n\nusing FlowFieldSpectra: FlowFieldSpectra as FFS\nusing Random: Random\nRandom.seed!(7)\n\nN = 250\nt = sort(rand(N) .* 10.0)                  # irregular sample times in [0, 10]\nf0 = 1.3\ny = @. sin(2π * f0 * t) + 0.3 * randn()\nfreqs = range(0.1, stop = 4.0, length = 400)\nP = FFS.lomb_scargle(t, collect(y), collect(freqs))\n\n(Image: Lomb–Scargle periodogram peaking at the true frequency f₀ = 1.3 despite irregular sampling)\n\nThe periodogram peaks at the true frequency f₀ = 1.3 despite the uneven sampling and noise.","title":"Lomb–Scargle (irregular sampling)"},{"category":"section","location":"examples/estimation/#Multitaper-(DPSS)-variance-reduction","page":"Irregular & windowed","text":"For a short, uniformly-sampled record, a single periodogram is noisy. The discrete prolate spheroidal sequences (dpss) form an orthogonal family of optimally band-limited tapers; averaging the spectra of the tapered signal yields a low-variance estimate. The taper spectra stack along the realization axis and reuse the Welch averaging path.\n\nusing FFTW: FFTW                          # activates the FFT extension\nusing SpectralBackends: SpectralBackends as SB\nusing FlowGeometries: FlowGeometries as FG\n\nNx = 256\nL = 2π\nx = range(0.0, L; length = Nx + 1)[1:Nx]\nfreq = [0:(Nx ÷ 2 - 1); -(Nx ÷ 2):-1]\nbg = real(FFTW.ifft(FFTW.fft(randn(Nx)) .* [k == 0 ? 0.0 : abs(k)^(-1.0) for k in freq]))\nk0 = 20\nsig = @. 0.25 * cos(k0 * x) + bg            # tone at k₀ on a k⁻² (red-noise) continuum\n\nK = 6\nV = FFS.dpss(Nx, 4.0, K)                    # N×K taper matrix (NW = 4)\ngrid = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), x; periodic = (true,), period = (L,))\nC = zeros(ComplexF64, Nx, K)\nfor k in 1:K\n    c, ks1 = FFS.calculate_spectrum(grid, V[:, k] .* sig, (Nx,); transform = SB.FFTSpectralBackend())\n    C[:, k] .= c\n    global ks = ks1\nend\nkb, Emt = FFS.welch_power_spectrum(ks, C; num_bins = 48)\n\n# Compare against a single taper computed the same way, so only the variance differs (the tapers\n# carry a normalization that a raw periodogram would not, which would offset the levels).\nkb1, Esingle = FFS.welch_power_spectrum(ks, C[:, 1:1]; num_bins = 48)\n\nThe multitaper estimate is markedly smoother than a single taper at the same level, while preserving the spectral peak at k₀ = 20.","title":"Multitaper (DPSS) variance reduction"},{"category":"section","location":"examples/nufft_coastline/#NUFFT:-jittered-points-and-a-coastline-cutout","page":"NUFFT & coastline cutout","text":"Real observational data rarely sit on a clean grid, and part of the domain is often missing (land, gaps, masked regions). The non-uniform FFT (FINUFFT) recovers the spectrum directly from a scattered, masked point cloud. Here we jitter the sample locations off the grid and remove \"land\" with an analytic coastline (a corner island plus a wavy coast), then compare the recovered spectrum against the true spectrum from the full uniform grid.\n\nusing FlowFieldSpectra: FlowFieldSpectra as FFS\nusing FFTW: FFTW                          # activates the FFT extension\nusing FINUFFT: FINUFFT                    # activates the NUFFT extension\nusing Random: Random\nusing SpectralBackends: SpectralBackends as SB\nusing FlowGeometries: FlowGeometries as FG\nRandom.seed!(42)\n\nL = 2π\nN = 64\ndx = L / N\nxs = range(0.0, L; length = N + 1)[1:N]\n\nfield(x, y) = cos(3x) + 0.6 * sin(5y) + 0.4 * cos(2x + 4y)\n\n# Reference spectrum on the full uniform grid (a plain (N, N) field tensor).\ngrid = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), xs, xs;\n    periodic = (true, true), period = (L, L))\nF = [field(x, y) for x in xs, y in xs]\nc_fft, k_fft = FFS.calculate_spectrum(grid, F, (N, N); transform = SB.FFTSpectralBackend())\nk_ref, E_ref = FFS.isotropic_spectrum(k_fft, c_fft; num_bins = 24)\n\n# Jitter the grid points off-grid, then cut out \"land\".\nxv = vec([x for x in xs, y in xs])\nyv = vec([y for x in xs, y in xs])\nxj = clamp.(xv .+ (rand(length(xv)) .- 0.5) .* (0.4dx), 0.0, L)\nyj = clamp.(yv .+ (rand(length(yv)) .- 0.5) .* (0.4dx), 0.0, L)\nis_land(x, y) = ((x)^2 + (y)^2 < (0.45L)^2) || (y < 0.18L + 0.10L * sin(4π * x / L))\nocean = .!is_land.(xj, yj)\nxo, yo = xj[ocean], yj[ocean]\nfo = field.(xo, yo)                       # scattered field: a length-M vector\n\n# NUFFT on the irregular ocean-only cloud (an unstructured Cartesian grid).\nocean_grid = FG.Grids.UnstructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), (xo, yo),\n    ones(length(xo)); periodic = (true, true), period = (L, L))\nc_nu, k_nu = FFS.calculate_spectrum(ocean_grid, fo, (N, N); transform = FFS.FINUFFTBackend(), eps = 1e-9)\nk_b, E_nu = FFS.isotropic_spectrum(k_nu, c_nu; num_bins = 24)\n\n(Image: Ocean-only sample cloud and the NUFFT-recovered spectrum vs the full-grid FFT)","title":"NUFFT: jittered points & a coastline cutout"},{"category":"section","location":"examples/spherical/#Spherical-harmonic-spectra","page":"Spherical harmonics","text":"Project a field on the sphere onto spherical harmonics and read off the degree (ℓ) energy spectrum. We synthesize a field from known modes Y₂¹ and Y₅⁻³ on a Clenshaw–Curtis grid and recover them with the structured FSHTSpectralBackend.\n\nusing FlowFieldSpectra: FlowFieldSpectra as FFS\nusing FastSphericalHarmonics: FastSphericalHarmonics as FSH\nusing SpectralBackends: SpectralBackends as SB\nusing FlowGeometries: FlowGeometries as FG\n\nlmax = 16\nNθ = lmax + 1\nNφ = 2 * lmax + 1\n\n# Clenshaw–Curtis grid (FlowGeometries) — the grid FastSphericalHarmonics transforms on. Fields are\n# (nlon, nlat) tensors in FlowGeometries' longitude-first layout.\ngrid = FG.Connectivity.structured_grid(Float64, FG.SphericalSampling.ClenshawCurtisSampling(), Nθ)\n\nC = zeros(ComplexF64, Nθ, Nφ)\nC[FFS.sph_mode_index(2, 1)] = 1.0\nC[FFS.sph_mode_index(5, -3)] = 0.5\nf = real(FFS.synthesize(grid, C, (Nθ, Nφ)))       # (nlon, nlat) field on the grid\n\ncoeffs, _ = FFS.calculate_spectrum(grid, f, (Nθ, Nφ); transform = SB.FSHTSpectralBackend())\ndeg, E_l = FFS.spherical_energy_spectrum(coeffs)\n\n(Image: Degree energy spectrum E(ℓ) with energy only at ℓ = 2 and ℓ = 5)\n\nEnergy appears only at degrees ℓ = 2 and ℓ = 5, as expected. Scattered points on the sphere are handled the same way with an FG.Grids.UnstructuredGrid over a SphericalGeometry and the NUFSHTSpectralBackend (use a well-distributed set such as a Fibonacci sphere so the least-squares solve is well-conditioned).","title":"Spherical harmonic spectra"},{"category":"section","location":"examples/cartesian/#Cartesian-spectra-(FFT)","page":"Cartesian (FFT)","text":"A synthetic incompressible turbulent field with a Kolmogorov k⁻⁵ᐟ³ energy cascade, its 2D energy-density map, and the various 1D reductions. The figures are rendered by the package's docs/generate_assets/generate_assets.jl script; the code shown here reproduces them.\n\nusing FlowFieldSpectra: FlowFieldSpectra as FFS\nusing FFTW: FFTW                          # activates the FFT extension\nusing Random: Random\nusing SpectralBackends: SpectralBackends as SB\nusing FlowGeometries: FlowGeometries as FG\n\nL = 2π\nN = 64\nxs = range(0.0, L; length = N + 1)[1:N]\n\n# Synthetic turbulence: a broadband streamfunction ψ, then u = ∂ψ/∂y, v = -∂ψ/∂x (spectral\n# derivatives), so the velocity is incompressible with an E(k) ∝ k⁻⁵ᐟ³ cascade.\nRandom.seed!(1)\nfreq = [0:(N ÷ 2 - 1); -(N ÷ 2):-1] .* (2π / L)        # FFTW-order wavenumbers\nψ̂ = FFTW.fft(randn(N, N))\nfor j in 1:N, i in 1:N\n    k = hypot(freq[i], freq[j])\n    ψ̂[i, j] *= k == 0 ? 0.0 : k^(-(11 / 3 + 1) / 2)\nend\nu = real(FFTW.ifft([im * freq[j] * ψ̂[i, j] for i in 1:N, j in 1:N]))    # (N, N) field tensors\nv = real(FFTW.ifft([-im * freq[i] * ψ̂[i, j] for i in 1:N, j in 1:N]))\n\n# A uniform periodic Cartesian grid (FlowGeometries) defined by its axes; fields are (N, N) tensors.\ngrid = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), xs, xs;\n    periodic = (true, true), period = (L, L))\ncoeffs, ks = FFS.calculate_spectrum(grid, (u, v), (N, N); transform = SB.FFTSpectralBackend())","title":"Cartesian spectra (FFT)"},{"category":"section","location":"examples/cartesian/#Isotropic-(radial)-energy-spectrum","page":"Cartesian (FFT)","text":"# Kinetic energy: fold the component axis (dim 3) into the radial spectrum.\nk_bins_iso, E_k = FFS.isotropic_spectrum(ks, coeffs; num_bins = 32, dims = 3)\nrng = 2:findlast(<=(0.6 * maximum(k_bins_iso)), k_bins_iso)   # resolved inertial range\n\n(Image: Isotropic energy spectrum with a k⁻⁵ᐟ³ Kolmogorov reference slope)","title":"Isotropic (radial) energy spectrum"},{"category":"section","location":"examples/cartesian/#Transect-spectrum","page":"Cartesian (FFT)","text":"Integrate out the second axis to get the 1D (zonal) spectrum along the first. Unlike the isotropic spectrum, this keeps the signed kₓ axis:\n\nk_red, E_red = FFS.transect_spectrum(ks, coeffs, (2,))","title":"Transect spectrum"},{"category":"section","location":"examples/cartesian/#Anisotropy-resolved-spectrum-E(k,-θ)","page":"Cartesian (FFT)","text":"isotropic_spectrum averages over direction; anisotropic_spectrum resolves it. We build a directionally biased broadband field (energy stretched toward one axis) so the preferred orientation shows up as a band in the (k, θ) plane.\n\nĝ = FFTW.fft(randn(N, N))\nfor j in 1:N, i in 1:N\n    kk = hypot(3.5 * freq[i], freq[j] / 3.5)           # anisotropic weighting\n    ĝ[i, j] *= kk == 0 ? 0.0 : kk^(-(0.6 + 1) / 2)\nend\ng = real(FFTW.ifft(ĝ))                                 # (N, N) scalar field\ncg, _ = FFS.calculate_spectrum(grid, g, (N, N); transform = SB.FFTSpectralBackend())\nk_bins, θ_bins, Ekθ = FFS.anisotropic_spectrum(ks, cg; num_k_bins = 24, num_θ_bins = 28)\n\n(Image: Anisotropy-resolved spectrum E(k, θ))","title":"Anisotropy-resolved spectrum E(k, θ)"},{"category":"section","location":"examples/cartesian/#Compensated-and-band-integrated-spectra","page":"Cartesian (FFT)","text":"compensate(k, E, p) forms kᵖ E(k); the k⁵ᐟ³ compensation flattens a Kolmogorov inertial range into a plateau, making the cascade easy to verify. band_energy integrates E(k) over a band.\n\nE_comp = FFS.compensate(k_bins_iso, E_k, 5 / 3)\nband = FFS.band_energy(k_bins_iso, E_k, 1.0, 8.0)\n@show band","title":"Compensated & band-integrated spectra"},{"category":"section","location":"examples/cartesian/#Synthesis-and-spectral-filtering","page":"Cartesian (FFT)","text":"synthesize is the inverse transform. Zeroing the high-wavenumber coefficients and synthesizing back gives a low-pass-filtered field — an exact round-trip on a uniform grid.\n\n# Low-pass: keep only |k| ≲ 4 by masking the shifted coefficient grid.\nkx, ky = ks\nmask = [sqrt(kx[i]^2 + ky[j]^2) <= 4.0 for i in 1:N, j in 1:N]\ncfilt = copy(coeffs)\nfor c in axes(cfilt, 3)\n    @views cfilt[:, :, c] .*= mask\nend\nu_lp = FFS.synthesize(grid, cfilt, (N, N))[:, :, 1]   # low-passed u-component field","title":"Synthesis & spectral filtering"},{"category":"section","location":"examples/derived_quantities/#Derived-quantity-spectra-(vorticity,-divergence,-enstrophy)","page":"Derived quantities","text":"FlowFieldSpectra forms the kinetic-energy spectrum from a velocity field; the same machinery, via spectral differentiation (ik^α), gives the spectra of vorticity, divergence, and enstrophy. To show the canonical relationship we use a synthetic incompressible turbulent flow with a Kolmogorov k⁻⁵ᐟ³ energy cascade — for which the enstrophy spectrum is Z(k) = k² E(k) ∝ k⁺¹ᐟ³.\n\nusing FlowFieldSpectra: FlowFieldSpectra as FFS\nusing FFTW: FFTW                          # activates the FFT extension\nusing Random: Random\nusing SpectralBackends: SpectralBackends as SB\nusing FlowGeometries: FlowGeometries as FG\n\nL = 2π\nN = 128\nxs = range(0.0, L; length = N + 1)[1:N]\n\n# Synthetic incompressible turbulence: build a broadband streamfunction ψ, then derive the\n# velocity by spectral differentiation u = ∂ψ/∂y, v = -∂ψ/∂x (so ∇·u = 0 by construction).\n# ψ's slope is chosen so the velocity energy follows a k⁻⁵ᐟ³ cascade.\nRandom.seed!(1)\nfreq = [0:(N ÷ 2 - 1); -(N ÷ 2):-1] .* (2π / L)        # FFTW-order wavenumbers\nψ̂ = FFTW.fft(randn(N, N))\nfor j in 1:N, i in 1:N\n    k = hypot(freq[i], freq[j])\n    ψ̂[i, j] *= k == 0 ? 0.0 : k^(-(11 / 3 + 1) / 2)\nend\nu = real(FFTW.ifft([im * freq[j] * ψ̂[i, j] for i in 1:N, j in 1:N]))    # (N, N) field tensors\nv = real(FFTW.ifft([-im * freq[i] * ψ̂[i, j] for i in 1:N, j in 1:N]))\n\ngrid = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), xs, xs;\n    periodic = (true, true), period = (L, L))\ncoeffs, ks = FFS.calculate_spectrum(grid, (u, v), (N, N); transform = SB.FFTSpectralBackend())\n\n# Spectral operators: divergence ≈ 0 (incompressible); vorticity → enstrophy spectrum.\ndivc = FFS.spectral_divergence(ks, coeffs)\nvortc = FFS.spectral_vorticity(ks, coeffs)\n@show maximum(abs, divc)                   # ~machine epsilon: the field is divergence-free\n\n# Fold the trailing component axis (dim 3) into each energy spectrum.\nk, E = FFS.isotropic_spectrum(ks, coeffs; num_bins = 40, dims = 3)\n_, Z = FFS.isotropic_spectrum(ks, vortc; num_bins = 40, dims = 3)\n\nrng = 2:findlast(<=(0.6 * maximum(k)), k)  # resolved inertial range\n\n(Image: Energy and enstrophy spectra with the Z(k) = k²E(k) identity check)\n\nE(k) and Z(k) are well-separated power laws with opposite slopes, and the k² E(k) markers fall exactly on the directly-computed enstrophy Z(k) — the Z = k²E identity, a validation cross-check for the spectral-curl operator on an incompressible field.","title":"Derived-quantity spectra (vorticity, divergence, enstrophy)"},{"category":"section","location":"internals/#Internals","page":"Internals","text":"These functions and types are not part of the public API (they are not exported), but are documented here for contributors. They may change without notice.","title":"Internals"},{"category":"section","location":"internals/#Execution-backend-helpers","page":"Internals","text":"The execution-axis introspection helpers (local_backend, is_distributed, resolve_backend) are provided by ComputationalBackends.jl and used internally and by the distribution extensions. The default AutoBackend is resolved locally through an internal _resolve_execution (Threaded when OhMyThreads is loaded and Threads.nthreads() > 1, else Serial), so precompilation never calls ComputationalBackends' resolve_backend(::AutoBackend) (which errors by design).","title":"Execution-backend helpers"},{"category":"section","location":"internals/#Grids","page":"Internals","text":"FlowFieldSpectra builds on FlowGeometries' grid/geometry types and reads them through FlowGeometries' own accessors; the only grid-related internals are the spectral wavenumber grid and the spherical (θ, φ) / quadrature-weight bridge (FlowGeometries stores (λ, φ_lat); the transforms want (θ = colatitude, φ = longitude)).","title":"Grids"},{"category":"section","location":"internals/#Transform-problem-and-layout","page":"Internals","text":"","title":"Transform problem & layout"},{"category":"section","location":"internals/#Preprocessing-helpers","page":"Internals","text":"","title":"Preprocessing helpers"},{"category":"section","location":"internals/#Normalization-helpers","page":"Internals","text":"","title":"Normalization helpers"},{"category":"section","location":"internals/#Spherical-harmonic-kernels","page":"Internals","text":"","title":"Spherical-harmonic kernels"},{"category":"function","location":"internals/#FlowFieldSpectra.Grids.physical_wavenumbers","page":"Internals","text":"physical_wavenumbers(grid, ms::NTuple{D,Int}) -> NTuple{D,<:AbstractRange}\n\nPhysical wavenumber ranges for a Cartesian grid at spectral resolution ms. The Fourier period L per axis is FlowGeometries' period(grid, d) (2π/L scaling); a non-periodic direction reports period 0 and is treated as L = 1 (raw per-sample wavenumbers).\n\n\n\n\n\n","title":"FlowFieldSpectra.Grids.physical_wavenumbers"},{"category":"function","location":"internals/#FlowFieldSpectra.Grids._sph_points","page":"Internals","text":"_sph_points(grid) -> (θ, φ)\n\nPer-point colatitude/longitude lists for a spherical grid. A structured (nlon, nlat) grid is expanded in FlowGeometries' column-major order (longitude fastest, matching size); a scattered grid returns its nodes. Both in the transform's (θ = colatitude, φ = longitude) convention.\n\n\n\n\n\n","title":"FlowFieldSpectra.Grids._sph_points"},{"category":"function","location":"internals/#FlowFieldSpectra.Grids._sht_weights","page":"Internals","text":"_sht_weights(grid, nlat; sampling=nothing, weights=nothing) -> Union{Nothing,Vector}\n\nPer-colatitude quadrature weights for a spherical grid: an explicit weights vector if given; otherwise those of the FlowGeometries sampling via SphericalSampling.latitude_weights (Σw = 2, carrying the sinθ Jacobian — exactly the transform's convention); otherwise nothing (uniform).\n\n\n\n\n\n","title":"FlowFieldSpectra.Grids._sht_weights"},{"category":"function","location":"internals/#FlowFieldSpectra.Problem.spatial_shape","page":"Internals","text":"spatial_shape(prob) — the leading spatial array sizes (N_1, …).\n\n\n\n\n\n","title":"FlowFieldSpectra.Problem.spatial_shape"},{"category":"function","location":"internals/#FlowFieldSpectra.Problem.batch_shape","page":"Internals","text":"batch_shape(prob) — the trailing batch sizes (() if none).\n\n\n\n\n\n","title":"FlowFieldSpectra.Problem.batch_shape"},{"category":"function","location":"internals/#FlowFieldSpectra.Problem.n_batch","page":"Internals","text":"n_batch(prob) — number of trailing batch dims.\n\n\n\n\n\n","title":"FlowFieldSpectra.Problem.n_batch"},{"category":"function","location":"internals/#FlowFieldSpectra.Problem.batch_length","page":"Internals","text":"batch_length(prob) — total batch slices, prod(batch) (1 if no batch axes).\n\n\n\n\n\n","title":"FlowFieldSpectra.Problem.batch_length"},{"category":"function","location":"internals/#FlowFieldSpectra.Problem.coeff_output_size","page":"Internals","text":"coeff_output_size(spectral::Tuple, prob) -> NTuple\n\nShape of the coefficient array: (spectral…, batch…).\n\n\n\n\n\n","title":"FlowFieldSpectra.Problem.coeff_output_size"},{"category":"function","location":"internals/#FlowFieldSpectra.Problem.stack_fields","page":"Internals","text":"stack_fields(fields::Tuple) -> AbstractArray\n\nConvenience for the multi-field call form: stack equal-shaped field arrays along a new trailing batch axis ((spatial…, batch…, NU)). This materializes a combined array — pass a single (spatial…, batch…) array to avoid the copy.\n\n\n\n\n\n","title":"FlowFieldSpectra.Problem.stack_fields"},{"category":"function","location":"internals/#FlowFieldSpectra.Preprocessing.window_function","page":"Internals","text":"window_function(win::AbstractWindow, n::Integer, ::Type{T}=Float64) -> Vector{T}\n\nAllocate and return the length-n taper win.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.window_function"},{"category":"function","location":"internals/#FlowFieldSpectra.Preprocessing.window_function!","page":"Internals","text":"window_function!(w::AbstractVector, win::AbstractWindow) -> w\n\nFill w (length n) in place with the taper win.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.window_function!"},{"category":"function","location":"internals/#FlowFieldSpectra.Preprocessing.window_correction","page":"Internals","text":"window_correction(w::AbstractVector) -> (S1, S2)\n\nCoherent-gain factor S1 = (Σ w)/n and power factor S2 = (Σ w²)/n. Amplitude spectra divide by S1; power/energy spectra divide by S2 to preserve variance.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.window_correction"},{"category":"function","location":"internals/#FlowFieldSpectra.Preprocessing.detrend!","page":"Internals","text":"detrend!(x::AbstractVector, d::AbstractDetrend) -> x\n\nDetrend x in place according to d.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.detrend!"},{"category":"function","location":"internals/#FlowFieldSpectra.Normalization.sided_factor","page":"Internals","text":"sided_factor(s::AbstractSidedness, k, kmax) -> Real\n\nFolding multiplier. TwoSided → 1 everywhere. OneSided → 2 for interior wavenumbers, 1 at DC (k≈0) and Nyquist (k≈kmax) which have no negative-frequency partner.\n\n\n\n\n\n","title":"FlowFieldSpectra.Normalization.sided_factor"},{"category":"type","location":"internals/#FlowFieldSpectra.SphericalKernels.LegendreTables","page":"Internals","text":"LegendreTables{FT}\n\nPrecomputed, point-independent recurrence coefficients for the normalized associated Legendre functions bar P_ell^m up to degree lmax. Built once per transform via legendre_tables; the per-point table is then filled in O(lmax²) with no sqrt calls by fill_legendre!.\n\n\n\n\n\n","title":"FlowFieldSpectra.SphericalKernels.LegendreTables"},{"category":"function","location":"internals/#FlowFieldSpectra.SphericalKernels.legendre_tables","page":"Internals","text":"legendre_tables(::Type{FT}, lmax::Int) -> LegendreTables{FT}\n\nPrecompute the recurrence coefficients up to degree lmax.\n\n\n\n\n\n","title":"FlowFieldSpectra.SphericalKernels.legendre_tables"},{"category":"function","location":"internals/#FlowFieldSpectra.SphericalKernels.fill_legendre!","page":"Internals","text":"fill_legendre!(Plm::AbstractMatrix, t::LegendreTables, x, s, lmax)\n\nFill Plm[l+1, m+1] = \\bar P_\\ell^m(x) for m = 0:lmax, l = m:lmax at a single point with x = cosθ, s = sinθ, reusing the precomputed coefficients in t. Entries with l < m are left untouched (the projection never reads them).\n\n\n\n\n\n","title":"FlowFieldSpectra.SphericalKernels.fill_legendre!"},{"category":"function","location":"internals/#FlowFieldSpectra.SphericalKernels.normalized_legendre","page":"Internals","text":"normalized_legendre(l, m, x, s) -> FT\n\nSingle normalized associated Legendre value bar P_ell^m(x) (m ≥ 0) computed by on-the-fly recurrence. Reference implementation used for validation; the hot path uses fill_legendre!.\n\n\n\n\n\n","title":"FlowFieldSpectra.SphericalKernels.normalized_legendre"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Spectra","page":"API Reference","text":"","title":"Spectra"},{"category":"section","location":"api/#Grids","page":"API Reference","text":"The coordinate system is the grid type — construct the grid that matches your data. Grids come from FlowGeometries.jl: a Cartesian grid is a FG.Grids.StructuredGrid / FG.Grids.UnstructuredGrid over a FG.Geometry.CartesianGeometry, and a spherical grid the same over a FG.Geometry.SphericalGeometry. Structured vs. scattered is the grid architecture, and uniform vs. non-uniform is the FG.Grids.isuniform value trait (an AbstractRange axis is uniform, a Vector is not) — there are no separate FlowFieldSpectra grid types. Spherical sampling schemes (ClenshawCurtisSampling, GaussLegendreSampling, DriscollHealySampling) live in FG.SphericalSampling; build a structured spherical grid with FG.Connectivity.structured_grid. FlowFieldSpectra reads every grid through FlowGeometries' own accessors (size, ndims, length, coordinates, isuniform, period, …). See the FlowGeometries documentation for construction.","title":"Grids"},{"category":"section","location":"api/#Reductions","page":"API Reference","text":"","title":"Reductions"},{"category":"section","location":"api/#Cross-spectra","page":"API Reference","text":"","title":"Cross-spectra"},{"category":"section","location":"api/#Averaging-(variance-reduction,-coherence-and-phase)","page":"API Reference","text":"","title":"Averaging (variance reduction, coherence & phase)"},{"category":"section","location":"api/#Derived-quantities-and-post-processing","page":"API Reference","text":"","title":"Derived quantities & post-processing"},{"category":"section","location":"api/#Preprocessing-and-normalization-conventions","page":"API Reference","text":"","title":"Preprocessing & normalization conventions"},{"category":"section","location":"api/#Transform-backends-(which-spectral-math)","page":"API Reference","text":"The two backend axes are orthogonal and compose: pass one transform= and one execution=. See Backends and Extensions for the selection matrix and profiles.\n\nThe transform= keyword takes a marker tag from SpectralBackends.jl: DirectSumSpectralBackend (the default), FFTSpectralBackend, NUFFTSpectralBackend, FSHTSpectralBackend, NUFSHTSpectralBackend (all <: SpectralBackends.AbstractSpectralBackend). Transform options such as eps, tol, iflag, and solve are calculate_spectrum keyword arguments.\n\nA NUFFT provider is a library choice, not spectral math, so FlowFieldSpectra owns two symmetric, concrete NUFFT backends (neither is a default; SpectralBackends.NUFFTSpectralBackend selects neither):","title":"Transform backends (which spectral math)"},{"category":"section","location":"api/#Execution-backends-(where/how-it-runs)","page":"API Reference","text":"The execution= keyword takes a tag from ComputationalBackends.jl: SerialBackend, ThreadedBackend, GPUBackend, DistributedBackend, MPIBackend, and AutoBackend (the default, resolved locally to ThreadedBackend/SerialBackend), all <: ComputationalBackends.AbstractExecutionBackend.","title":"Execution backends (where/how it runs)"},{"category":"section","location":"api/#Plotting-and-analysis","page":"API Reference","text":"These require CairoMakie to be loaded.","title":"Plotting & analysis"},{"category":"function","location":"api/#FlowFieldSpectra.calculate_spectrum","page":"API Reference","text":"calculate_spectrum(grid, field, ms::Tuple;\n                   transform=DirectSumSpectralBackend(), execution=AutoBackend(), kwargs...)\n\nSpectral coefficients and physical wavenumbers of field sampled on grid (a FlowGeometries grid). The grid's architecture × geometry is the coordinate system — no coordinate guessing. The two backend axes compose freely:\n\ntransform::SpectralBackends.AbstractSpectralBackend — the spectral math (DirectSumSpectralBackend (default), FFTSpectralBackend, NUFFTSpectralBackend, FSHTSpectralBackend, NUFSHTSpectralBackend).\nexecution::ComputationalBackends.AbstractExecutionBackend — where/how it runs (SerialBackend, ThreadedBackend, GPUBackend, DistributedBackend, MPIBackend, AutoBackend (default)).\n\nData model\n\nfield is an AbstractArray shaped (spatial…, batch…): the first ndims(grid) dims are spatial (and must equal size(grid)); every trailing dim is a batch dim (components, levels, time, ensemble — any number), carried through and preserved. Structured grids take an (N_1,…,N_D, batch…) tensor (spherical: (nlon, nlat, batch…)); unstructured grids take (N, batch…). A Tuple of equal-shaped arrays stacks them along a new trailing batch axis.\n\nms is the spectral resolution: Cartesian (m_1,…,m_D); spherical (Nθ, Nφ) with lmax = Nθ-1.\n\nReturns\n\n(coeffs, ks_phys) — complex coefficients (ms…, batch…) (Cartesian) / (Nθ, Nφ, batch…) (spherical), and the physical wavenumber coordinates per spectral axis.\n\n\n\n\n\n","title":"FlowFieldSpectra.calculate_spectrum"},{"category":"function","location":"api/#FlowFieldSpectra.calculate_spectrum!","page":"API Reference","text":"calculate_spectrum!(coeffs, grid, field, ms; transform=DirectSumSpectralBackend(), execution=AutoBackend(), kwargs...)\n\nIn-place calculate_spectrum: write coefficients (ms…, batch…) into the preallocated coeffs and return ks_phys. Supported for transform=DirectSumSpectralBackend() with SerialBackend/ThreadedBackend; for the library transforms build a reusable plan with plan_spectrum and call calculate_spectrum!(coeffs, plan, field).\n\n\n\n\n\n","title":"FlowFieldSpectra.calculate_spectrum!"},{"category":"function","location":"api/#FlowFieldSpectra.synthesize","page":"API Reference","text":"synthesize(grid, coeffs, ms::Tuple; transform=DirectSumSpectralBackend(), execution=AutoBackend(),\n           real_output=true, iflag=1)\n\nInverse of calculate_spectrum: reconstruct field values at the grid points from coefficients coeffs ((ms…, batch…) Cartesian, (Nθ, Nφ, batch…) spherical). Returns an array (spatial…, batch…); real_output=true returns its real part. Uses the direct-sum inverse (works for any grid) with SerialBackend/ThreadedBackend.\n\n\n\n\n\n","title":"FlowFieldSpectra.synthesize"},{"category":"function","location":"api/#FlowFieldSpectra.Plans.plan_spectrum","page":"API Reference","text":"plan_spectrum(grid, ::Type{T}, ms; transform=DirectSumSpectralBackend(), execution=AutoBackend(), batch=(), kwargs...)\nplan_spectrum(transform, execution, grid, ::Type{T}, ms; batch=(), kwargs...)\n\nConstruct a reusable AbstractSpectralPlan for the transform×execution backend pair on grid at spectral resolution ms, transforming a field with trailing batch shape batch of element type T in one batched execution. The keyword form resolves execution and forwards to the canonical positional form implemented by the backend extensions. Requires the transform's extension to be loaded.\n\nExecute a plan with calculate_spectrum!(coeffs, plan, fields).\n\n\n\n\n\n","title":"FlowFieldSpectra.Plans.plan_spectrum"},{"category":"type","location":"api/#FlowFieldSpectra.Plans.AbstractSpectralPlan","page":"API Reference","text":"AbstractSpectralPlan\n\nSupertype for reusable transform plans. A plan is tied to the fixed geometry of a problem — the grid coordinates, the spectral resolution ms, the number of batched transforms n_transf, and the element type — but not to the field values. Build a plan once with plan_spectrum and reuse it across many fields / batch slices / time steps via calculate_spectrum!, avoiding repeated FFTW/FINUFFT plan construction and point sorting.\n\nConcrete plan types are defined in the backend extensions (e.g. the FFTW and FINUFFT extensions); this module only declares the shared interface.\n\n\n\n\n\n","title":"FlowFieldSpectra.Plans.AbstractSpectralPlan"},{"category":"function","location":"api/#FlowFieldSpectra.DirectSum.sph_mode_index","page":"API Reference","text":"sph_mode_index(l, m) -> CartesianIndex\n\nCartesianIndex of degree l, order m in the (lmax+1, 2lmax+1) coefficient array.\n\n\n\n\n\n","title":"FlowFieldSpectra.DirectSum.sph_mode_index"},{"category":"function","location":"api/#FlowFieldSpectra.Reductions.isotropic_spectrum","page":"API Reference","text":"isotropic_spectrum(ks_phys::Tuple, coeffs; num_bins=0, dims=())\n\n1D radially-integrated (isotropic) energy spectrum of coeffs (ms…, batch…), binning over the D = length(ks_phys) spectral dims and preserving all batch dims → (k_bins, E) with E of shape (num_bins, batch…). dims (absolute batch-dim index/indices > D) folds those axes into the energy (e.g. dims=D+1 sums a vector-component axis into a single kinetic-energy spectrum). E(k) = (1/2dk) Σ_{|k|∈bin} Σ_{folded} |C|².\n\n\n\n\n\n","title":"FlowFieldSpectra.Reductions.isotropic_spectrum"},{"category":"function","location":"api/#FlowFieldSpectra.Reductions.isotropic_spectrum!","page":"API Reference","text":"isotropic_spectrum!(E, k_bins, ks_phys, coeffs; num_bins=0)\n\nIn-place, allocation-free isotropic spectrum that preserves all batch dims (no folding): fills preallocated E (shape (num_bins, batch…)) and k_bins (length num_bins). Reusable across a time loop with zero steady-state heap traffic.\n\n\n\n\n\n","title":"FlowFieldSpectra.Reductions.isotropic_spectrum!"},{"category":"function","location":"api/#FlowFieldSpectra.Reductions.transect_spectrum","page":"API Reference","text":"transect_spectrum(ks_phys::Tuple, coeffs, dims::Tuple)\n\nIntegrate the spectral energy density ½|C|² along the spectral dimensions dims (1-indexed, ⊆ 1:D), scaling by their wavenumber spacing. Returns (ks_reduced, E_reduced) where E_reduced has the kept spectral dims followed by the batch dims.\n\n\n\n\n\n","title":"FlowFieldSpectra.Reductions.transect_spectrum"},{"category":"function","location":"api/#FlowFieldSpectra.Reductions.transect_spectrum!","page":"API Reference","text":"transect_spectrum!(E_reduced, ks_phys, coeffs, dims) -> nothing\n\nIn-place, allocation-free transect_spectrum: fills preallocated E_reduced (kept spectral dims + batch dims) with the dims-integrated ½|C|² density.\n\n\n\n\n\n","title":"FlowFieldSpectra.Reductions.transect_spectrum!"},{"category":"function","location":"api/#FlowFieldSpectra.Reductions.spherical_energy_spectrum","page":"API Reference","text":"spherical_energy_spectrum(coeffs; lmax=size(coeffs,1)-1)\n\nDegree energy spectrum E(ℓ, batch…) = ½ Σ_{m=-ℓ}^{ℓ} |C_ℓ^m|² of spherical-harmonic coefficients (Nθ, Nφ, batch…). Returns (0:lmax, E_l), E_l of shape (lmax+1, batch…).\n\n\n\n\n\n","title":"FlowFieldSpectra.Reductions.spherical_energy_spectrum"},{"category":"function","location":"api/#FlowFieldSpectra.Reductions.spherical_energy_spectrum!","page":"API Reference","text":"spherical_energy_spectrum!(E_l, coeffs; lmax=size(coeffs,1)-1) -> nothing\n\nIn-place spherical_energy_spectrum: fills preallocated E_l (shape (lmax+1, batch…)).\n\n\n\n\n\n","title":"FlowFieldSpectra.Reductions.spherical_energy_spectrum!"},{"category":"function","location":"api/#FlowFieldSpectra.Reductions.anisotropic_spectrum","page":"API Reference","text":"anisotropic_spectrum(ks_phys::Tuple, coeffs; num_k_bins=0, num_θ_bins=16, dims=())\n\nAnisotropy-resolved 2D energy spectrum E(k, θ, batch…) for a 2D field: bin ½|C|² by wavenumber magnitude and polar angle, preserving batch. Integrating over θ recovers the isotropic spectrum.\n\n\n\n\n\n","title":"FlowFieldSpectra.Reductions.anisotropic_spectrum"},{"category":"function","location":"api/#FlowFieldSpectra.Reductions.cross_spectrum","page":"API Reference","text":"cross_spectrum(ks_phys::Tuple, coeffs_f, coeffs_g; num_bins=0, dims=())\n\nRadially-binned cross-spectrum S_fg(k, batch…) = ½ Σ_{|k|∈bin} Σ_{folded} f̂ conj(ĝ); coeffs_f, coeffs_g share shape (ms…, batch…). Real part → co-spectrum, negative imag part → quad spectrum.\n\n\n\n\n\n","title":"FlowFieldSpectra.Reductions.cross_spectrum"},{"category":"function","location":"api/#FlowFieldSpectra.Reductions.cospectrum","page":"API Reference","text":"cospectrum(ks, cf, cg; …) — Re S_fg(k, batch…) (in-phase, flux-carrying part).\n\n\n\n\n\n","title":"FlowFieldSpectra.Reductions.cospectrum"},{"category":"function","location":"api/#FlowFieldSpectra.Reductions.quadspectrum","page":"API Reference","text":"quadspectrum(ks, cf, cg; …) — -Im S_fg(k, batch…) (90°-out-of-phase part).\n\n\n\n\n\n","title":"FlowFieldSpectra.Reductions.quadspectrum"},{"category":"function","location":"api/#FlowFieldSpectra.Averaging.welch_power_spectrum","page":"API Reference","text":"welch_power_spectrum(ks_phys::Tuple, coeffs; num_bins=0)\n\nVariance-reduced (Welch / ensemble-averaged) isotropic power spectrum. The trailing batch dims of coeffs (ms…, realization…) index independent segments/realizations whose periodograms are averaged before radial binning. Returns (k_bins, E_k).\n\n\n\n\n\n","title":"FlowFieldSpectra.Averaging.welch_power_spectrum"},{"category":"function","location":"api/#FlowFieldSpectra.Averaging.welch_power_spectrum!","page":"API Reference","text":"welch_power_spectrum!(E_k, k_bins, ks_phys, coeffs; num_bins=0) -> nothing\n\nIn-place, allocation-free welch_power_spectrum: fills preallocated E_k and k_bins (both length num_bins). Reusable across a loop with zero steady-state heap traffic.\n\n\n\n\n\n","title":"FlowFieldSpectra.Averaging.welch_power_spectrum!"},{"category":"function","location":"api/#FlowFieldSpectra.Averaging.coherence_spectrum","page":"API Reference","text":"coherence_spectrum(ks_phys::Tuple, cf, cg; num_bins=0) -> (k_bins, coherence², phase)\n\nMagnitude-squared coherence gamma^2(k) = S_fg^2  (S_ff S_gg) and phase between two fields whose coefficients cf, cg share (ms…, realization…). Cross/auto spectra are averaged over the realization batch and over the modes in each radial bin before the ratio is formed.\n\n\n\n\n\n","title":"FlowFieldSpectra.Averaging.coherence_spectrum"},{"category":"function","location":"api/#FlowFieldSpectra.Averaging.coherence_spectrum!","page":"API Reference","text":"coherence_spectrum!(coherence², phase, k_bins, ks_phys, cf, cg; num_bins=0) -> nothing\n\nIn-place coherence_spectrum: fills preallocated coherence², phase, k_bins (each length num_bins). Uses O(num_bins) internal scratch for the complex cross-spectrum accumulator.\n\n\n\n\n\n","title":"FlowFieldSpectra.Averaging.coherence_spectrum!"},{"category":"function","location":"api/#FlowFieldSpectra.LombScargle.lomb_scargle","page":"API Reference","text":"lomb_scargle(t, y, freqs; center=true) -> Vector\n\nLomb–Scargle periodogram of an irregularly-sampled 1D series y at sample times/locations t, evaluated at the (strictly positive) frequencies freqs. This is the standard estimator for gappy / non-uniformly sampled records (moorings, drifters, satellite tracks, astronomical time series) where an FFT cannot be applied directly.\n\nWith center=true (default) the sample mean is removed first. The classic time-shift τ is chosen per frequency to make the cosine and sine bases orthogonal, giving a periodogram that is invariant to time translation:\n\nP(f) = tfrac12left frac(sum_j y_jcosomega(t_j-tau))^2sum_jcos^2omega(t_j-tau)\n                     + frac(sum_j y_jsinomega(t_j-tau))^2sum_jsin^2omega(t_j-tau) right\nquad omega = 2pi f \n\nThis is the direct O(NM) reference implementation (N samples, M frequencies); it needs no FFT. freqs must be positive (the f=0 term is undefined).\n\n\n\n\n\n","title":"FlowFieldSpectra.LombScargle.lomb_scargle"},{"category":"function","location":"api/#FlowFieldSpectra.LombScargle.lomb_scargle!","page":"API Reference","text":"lomb_scargle!(P, t, y, freqs; center=true) -> P\n\nIn-place lomb_scargle: writes the periodogram into preallocated P (length length(freqs)). Allocation-free — reuse P across many series that share freqs.\n\n\n\n\n\n","title":"FlowFieldSpectra.LombScargle.lomb_scargle!"},{"category":"function","location":"api/#FlowFieldSpectra.Operators.spectral_divergence","page":"API Reference","text":"spectral_divergence(ks_phys::Tuple, coeffs) -> AbstractArray\n\nSpectral divergence widehatnablacdot u = isum_d k_d hat u_d of a D-component vector field with coefficients (ms…, D, extra…). Returns (ms…, 1, extra…). Defined for D = 1, 2, 3.\n\n\n\n\n\n","title":"FlowFieldSpectra.Operators.spectral_divergence"},{"category":"function","location":"api/#FlowFieldSpectra.Operators.spectral_divergence!","page":"API Reference","text":"spectral_divergence!(out, ks_phys::Tuple, coeffs) -> out\n\nIn-place spectral_divergence: writes the divergence into preallocated out of shape (ms…, 1, extra…). Allocation-free — reuse out across a batch/time loop.\n\n\n\n\n\n","title":"FlowFieldSpectra.Operators.spectral_divergence!"},{"category":"function","location":"api/#FlowFieldSpectra.Operators.spectral_vorticity","page":"API Reference","text":"spectral_vorticity(ks_phys::Tuple, coeffs) -> AbstractArray\n\nSpectral vorticity hatomega = ik times hat u of a vector field with coefficients (ms…, D, extra…): D = 2 → scalar out-of-plane vorticity (ms…, 1, extra…); D = 3 → 3-component vorticity (ms…, 3, extra…).\n\n\n\n\n\n","title":"FlowFieldSpectra.Operators.spectral_vorticity"},{"category":"function","location":"api/#FlowFieldSpectra.Operators.spectral_vorticity!","page":"API Reference","text":"spectral_vorticity!(out, ks_phys::Tuple, coeffs) -> out\n\nIn-place spectral_vorticity: writes the vorticity into preallocated out — shape (ms…, 1, extra…) for D = 2, (ms…, 3, extra…) for D = 3. Allocation-free.\n\n\n\n\n\n","title":"FlowFieldSpectra.Operators.spectral_vorticity!"},{"category":"function","location":"api/#FlowFieldSpectra.Operators.compensate","page":"API Reference","text":"compensate(k_bins, E_k, p) -> AbstractArray\n\nCompensated spectrum k^p E(k) (e.g. p = 5/3 Kolmogorov plateau, p = 2 for Z(k)=k²E(k)). E_k may be (num_bins,) or (num_bins, batch…); k_bins broadcasts along the wavenumber axis.\n\n\n\n\n\n","title":"FlowFieldSpectra.Operators.compensate"},{"category":"function","location":"api/#FlowFieldSpectra.Operators.band_energy","page":"API Reference","text":"band_energy(k_bins, E_k, k1, k2) -> Real\n\nEnergy integrated over the wavenumber band k_1 k_2 via the trapezoidal rule over the bins whose centers fall in the band. E_k is a 1D spectrum (num_bins,).\n\n\n\n\n\n","title":"FlowFieldSpectra.Operators.band_energy"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.Preprocess","page":"API Reference","text":"Preprocess(; detrend=Demean(), window=NoWindow(), pad=1.0)\n\nPreprocessing applied to a field (per spectral axis) before transforming. Fields are typed (not symbols) so downstream code dispatches at compile time.\n\ndetrend::AbstractDetrend: Demean() (default), NoDetrend(), or LinearDetrend().\nwindow::AbstractWindow: NoWindow() (default), Hann(), Hamming(), Blackman(), Tukey(α).\npad::Float64: zero-padding factor (≥ 1); 1.0 means none.\n\nWindow power/amplitude corrections for variance preservation are applied by the normalization layer via window_correction.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.Preprocess"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.AbstractWindow","page":"API Reference","text":"AbstractWindow\n\nSupertype for apodization tapers applied per spectral axis before transforming. Concrete windows dispatch window_function!. Reduces spectral leakage for non-periodic data; only meaningful on uniform axes.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.AbstractWindow"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.NoWindow","page":"API Reference","text":"NoWindow() — rectangular window (all ones); no apodization.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.NoWindow"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.Hann","page":"API Reference","text":"Hann() — Hann (raised-cosine) taper.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.Hann"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.Hamming","page":"API Reference","text":"Hamming() — Hamming taper.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.Hamming"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.Blackman","page":"API Reference","text":"Blackman() — Blackman taper.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.Blackman"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.Tukey","page":"API Reference","text":"Tukey(alpha=0.5)\n\nTukey (tapered-cosine) window with taper fraction alpha ∈ [0,1]; alpha=0 is rectangular and alpha=1 is Hann.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.Tukey"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.AbstractDetrend","page":"API Reference","text":"AbstractDetrend\n\nSupertype for detrending operations applied before transforming. Concrete subtypes dispatch detrend!.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.AbstractDetrend"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.NoDetrend","page":"API Reference","text":"NoDetrend() — leave the data unchanged.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.NoDetrend"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.Demean","page":"API Reference","text":"Demean() — subtract the mean (remove the DC component). The default.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.Demean"},{"category":"type","location":"api/#FlowFieldSpectra.Preprocessing.LinearDetrend","page":"API Reference","text":"LinearDetrend() — subtract the least-squares linear trend.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.LinearDetrend"},{"category":"function","location":"api/#FlowFieldSpectra.Preprocessing.dpss","page":"API Reference","text":"dpss(N::Integer, NW::Real, K::Integer = floor(Int, 2NW) - 1; T = Float64) -> Matrix{T}\n\nDiscrete prolate spheroidal sequences (Slepian tapers): the K length-N sequences with maximal spectral concentration in the half-bandwidth W = NW/N, returned as an N×K orthonormal matrix whose column k is the order-(k-1) taper. NW is the time–bandwidth product (typical 2.5–4); K ≈ 2·NW − 1 tapers are usefully concentrated.\n\nMultitaper power spectral estimation reuses the ensemble machinery: apply each taper to the (demeaned) signal, transform the K tapered copies as a batch, and average their periodograms with [welch_power_spectrum]. The tapers are the eigenvectors of a symmetric tridiagonal matrix (Percival & Walden), computed here with LinearAlgebra.eigen.\n\n\n\n\n\n","title":"FlowFieldSpectra.Preprocessing.dpss"},{"category":"type","location":"api/#FlowFieldSpectra.Normalization.SpectralConvention","page":"API Reference","text":"SpectralConvention(; sided=OneSided(), scaling=DensityScaling(), parseval_check=false)\n\nConvention governing how spectral coefficients become reported spectra, so the package never silently guesses normalization. Fields are typed for compile-time dispatch.\n\nsided::AbstractSidedness: OneSided() (default) or TwoSided().\nscaling::AbstractScaling: DensityScaling() (default) or PowerScaling().\nparseval_check::Bool: when true, callers assert Σ E·Δk ≈ Var(field) (after mean removal) as a correctness self-test.\n\nThe variance-preservation property — ∫ E(k) dk = Var(f) after demeaning — is the invariant the test suite enforces across every backend and grid.\n\n\n\n\n\n","title":"FlowFieldSpectra.Normalization.SpectralConvention"},{"category":"type","location":"api/#FlowFieldSpectra.Normalization.AbstractSidedness","page":"API Reference","text":"AbstractSidedness\n\nWhether a spectrum keeps both signs of wavenumber (TwoSided) or folds negatives onto positives (OneSided). Dispatches sided_factor.\n\n\n\n\n\n","title":"FlowFieldSpectra.Normalization.AbstractSidedness"},{"category":"type","location":"api/#FlowFieldSpectra.Normalization.OneSided","page":"API Reference","text":"OneSided() — fold negative wavenumbers onto positives (doubles interior bins). The usual convention for real fields.\n\n\n\n\n\n","title":"FlowFieldSpectra.Normalization.OneSided"},{"category":"type","location":"api/#FlowFieldSpectra.Normalization.TwoSided","page":"API Reference","text":"TwoSided() — keep ± wavenumbers (no folding).\n\n\n\n\n\n","title":"FlowFieldSpectra.Normalization.TwoSided"},{"category":"type","location":"api/#FlowFieldSpectra.Normalization.AbstractScaling","page":"API Reference","text":"AbstractScaling\n\nWhether a reduced spectrum is reported as a spectral density (DensityScaling, divided by the bin width so ∫E dk recovers variance) or as per-bin/per-mode PowerScaling.\n\n\n\n\n\n","title":"FlowFieldSpectra.Normalization.AbstractScaling"},{"category":"type","location":"api/#FlowFieldSpectra.Normalization.DensityScaling","page":"API Reference","text":"DensityScaling() — spectral density (per unit wavenumber); ∫E dk = Var(f).\n\n\n\n\n\n","title":"FlowFieldSpectra.Normalization.DensityScaling"},{"category":"type","location":"api/#FlowFieldSpectra.Normalization.PowerScaling","page":"API Reference","text":"PowerScaling() — per-bin/per-mode power (no dk division).\n\n\n\n\n\n","title":"FlowFieldSpectra.Normalization.PowerScaling"},{"category":"type","location":"api/#FlowFieldSpectra.Problem.TransformProblem","page":"API Reference","text":"TransformProblem{NS, B}\n\nShape of one transform: the leading NS spatial array dims (matching the grid) and the trailing B batch dims. Built from (grid, field) via TransformProblem; drives buffer ranks and the coefficient output size.\n\n\n\n\n\n","title":"FlowFieldSpectra.Problem.TransformProblem"},{"category":"type","location":"api/#FlowFieldSpectra.FINUFFTBackend","page":"API Reference","text":"FINUFFTBackend()\nNonuniformFFTsBackend()\n\nThe concrete NUFFT providers (<: SpectralBackends.AbstractNUFFTSpectralBackend). A provider is a library choice, not spectral math, so these live in FlowFieldSpectra, not SpectralBackends. They are symmetric — neither is a default: FINUFFTBackend() uses FINUFFT (using FINUFFT; GPU via cuFINUFFT), NonuniformFFTsBackend() uses NonuniformFFTs (using NonuniformFFTs; real-data fast path, half memory). Both may be loaded at once. The abstract SpectralBackends.NUFFTSpectralBackend() selects no provider — pass one of these.\n\n\n\n\n\n","title":"FlowFieldSpectra.FINUFFTBackend"},{"category":"type","location":"api/#FlowFieldSpectra.NonuniformFFTsBackend","page":"API Reference","text":"See FINUFFTBackend — the NonuniformFFTs.jl NUFFT provider.\n\n\n\n\n\n","title":"FlowFieldSpectra.NonuniformFFTsBackend"},{"category":"function","location":"api/#FlowFieldSpectra.plot_spectrum","page":"API Reference","text":"plot_spectrum(ks_phys::Tuple, coeffs; title=\"Energy Spectrum\", kwargs...)\n\nPlot a 1D isotropic / 2D Cartesian / spherical-degree energy spectrum. Requires using CairoMakie.\n\n\n\n\n\n","title":"FlowFieldSpectra.plot_spectrum"},{"category":"function","location":"api/#FlowFieldSpectra.compare_spectra","page":"API Reference","text":"compare_spectra(spectra_list; labels, kwargs...)\n\nOverlay multiple 1D energy spectra. Requires using CairoMakie.\n\n\n\n\n\n","title":"FlowFieldSpectra.compare_spectra"},{"category":"function","location":"api/#FlowFieldSpectra.compare_spectral_analysis","page":"API Reference","text":"compare_spectral_analysis(true_coeffs, approx_coeffs; kwargs...)\n\nCoefficient comparison + error maps. Requires using CairoMakie.\n\n\n\n\n\n","title":"FlowFieldSpectra.compare_spectral_analysis"},{"category":"section","location":"examples/komega/#Wavenumber–frequency-E(k,-ω)","page":"Wavenumber–frequency E(k,ω)","text":"Time is just another spectral axis. Sampling a 1D field f(x, t) on a uniform space–time grid and transforming both axes gives the wavenumber–frequency spectrum E(k, ω), which separates propagating waves (energy on the dispersion line ω = c·k) from non-propagating turbulence.\n\nHere we superpose a propagating wave on a slower background. The package's synthesis convention is f = Σ C(k,ω) e^{+i(k·x + ω·t)}, so a wave written as cos(k₀x + ω₀t) has its spectral peaks on the ω = k diagonal (phase speed c = ω₀/k₀); a mode with a different phase speed lies off it.\n\nusing FlowFieldSpectra: FlowFieldSpectra as FFS\nusing FFTW: FFTW                          # activates the FFT extension\nusing Random: Random\nusing SpectralBackends: SpectralBackends as SB\nusing FlowGeometries: FlowGeometries as FG\nRandom.seed!(0)\n\nNx, Nt = 64, 64\nLx, Lt = 2π, 2π\nx = range(0.0, Lx; length = Nx + 1)[1:Nx]\nt = range(0.0, Lt; length = Nt + 1)[1:Nt]\n\nk0, ω0 = 6.0, 6.0                          # phase speed c = ω0/k0 = 1  → on the ω = k line\n# f(x, t) as an (Nx, Nt) tensor: a wave on the ω = k line plus a slower off-line background.\nf = [cos(k0 * xi + ω0 * ti) + 0.4 * cos(2 * xi + 1.0 * ti + 0.5) for xi in x, ti in t]\n\n# dim 1 = space (→ k), dim 2 = time (→ ω)\ngrid = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), x, t;\n    periodic = (true, true), period = (Lx, Lt))\ncoeffs, ks = FFS.calculate_spectrum(grid, f, (Nx, Nt); transform = SB.FFTSpectralBackend())\nkx, kω = ks\nEkω = abs2.(coeffs)\n\n(Image: Wavenumber–frequency spectrum E(k, ω) with the dominant peak on the ω = k dispersion line)\n\nThe dominant peak sits on the ω = k dispersion line at (k₀, ω₀) = (6, 6) (and its conjugate at (−6, −6)); the weaker background mode lies off the line at a slower phase speed. Building the same field on a fixed nonuniform horizontal grid with the plan reused across time is shown in the 4D fixed-grid example.","title":"Wavenumber–frequency E(k, ω)"},{"category":"section","location":"backends/#Backends-and-Extensions","page":"Backends & Extensions","text":"FlowFieldSpectra.jl separates spectral algorithms from library dependencies using Julia 1.9+'s package extensions mechanism. By default, the core package is extremely lightweight and has no heavy external compiled dependencies (like FFTW or FINUFFT).\n\nTo activate optimized, high-performance backends, you simply import the corresponding package in your workspace.\n\nBackends come in two orthogonal axes that compose freely — the public API takes one of each as the transform= and execution= keywords. The tag types live in two shared packages:\n\nTransform (SpectralBackends.AbstractSpectralBackend) — which spectral math: DirectSumSpectralBackend, FFTSpectralBackend, NUFFTSpectralBackend, FSHTSpectralBackend, NUFSHTSpectralBackend (from SpectralBackends.jl). Covered by the matrix below.\nExecution (ComputationalBackends.AbstractExecutionBackend) — where/how it runs: SerialBackend, ThreadedBackend, GPUBackend, DistributedBackend, MPIBackend, AutoBackend (from ComputationalBackends.jl). Covered in Execution backends below.\n\nusing SpectralBackends: SpectralBackends as SB\nusing ComputationalBackends: ComputationalBackends as CB\n\n# FFT transform, run threaded:\ncalculate_spectrum(grid, fields, ms; transform = SB.FFTSpectralBackend(), execution = CB.ThreadedBackend())\n# Fast NUFFT on a CUDA GPU (cuFINUFFT):\ncalculate_spectrum(grid, fields, ms; transform = FFS.FINUFFTBackend(), execution = CB.GPUBackend(CUDABackend()))\n\n","title":"Backends and Extensions"},{"category":"section","location":"backends/#Backend-Selection-Matrix-(transform-axis)","page":"Backends & Extensions","text":"Choose your backend based on the grid structure (structured vs. scattered/non-uniform) and coordinate system (Cartesian vs. Spherical):\n\nCoordinate System Grid Structure Baseline Backend Fast Backend Required Library\nCartesian Uniform / Regular DirectSumSpectralBackend() FFTSpectralBackend() using FFTW\nCartesian Scattered / Unstructured DirectSumSpectralBackend() FINUFFTBackend() using FINUFFT\nSpherical Structured (Clenshaw-Curtis) DirectSumSpectralBackend() FSHTSpectralBackend() using FastSphericalHarmonics\nSpherical Scattered / Unstructured DirectSumSpectralBackend() NUFSHTSpectralBackend() using NUFSHT\n\n","title":"Backend Selection Matrix (transform axis)"},{"category":"section","location":"backends/#Detailed-Backend-Profiles","page":"Backends & Extensions","text":"","title":"Detailed Backend Profiles"},{"category":"section","location":"backends/#DirectSumSpectralBackend","page":"Backends & Extensions","text":"Use Case: Reference calculations, small grids, or zero-dependency runs.\nMathematical Method: Direct Discrete Fourier Transform (DFT) summation or Spherical Harmonic Transform (SHT) direct integration via Legendre recurrence relations.\nComplexity: O(N cdot M), where N is the number of grid nodes and M is the number of spectral modes.\nDependencies: None.","title":"DirectSumSpectralBackend"},{"category":"section","location":"backends/#FFTSpectralBackend","page":"Backends & Extensions","text":"Use Case: Traditional uniform Cartesian grids (e.g. models on regular grids).\nMathematical Method: Fast Fourier Transform (FFT) via FFTW.jl.\nComplexity: O(N log N).\nDependencies: Requires using FFTW.","title":"FFTSpectralBackend"},{"category":"section","location":"backends/#NUFFT-—-FINUFFTBackend-/-NonuniformFFTsBackend","page":"Backends & Extensions","text":"Use Case: Scattered spatial points in Cartesian coordinates (e.g. ship tracks, sensor arrays, float data).\nMathematical Method: Non-Uniform Fast Fourier Transform (Type-1). SpectralBackends.NUFFTSpectralBackend is the abstract math; a provider is a library choice, so FlowFieldSpectra exposes two symmetric, concrete backends (neither is a default) — pass one as transform=:\nFINUFFTBackend() — via FINUFFT.jl (using FINUFFT; GPU via cuFINUFFT on CUDA).\nNonuniformFFTsBackend() — via NonuniformFFTs.jl (using NonuniformFFTs); a real-data fast path (real-to-complex FFT — ≈2× faster and half the memory when the field is real).\nComplexity: O(N log N + M log(1epsilon)).\nDependencies: using FINUFFT and/or using NonuniformFFTs. Both may be loaded at once.","title":"NUFFT — FINUFFTBackend / NonuniformFFTsBackend"},{"category":"section","location":"backends/#FSHTSpectralBackend","page":"Backends & Extensions","text":"Use Case: Regular spherical model grids (equiangular, Clenshaw-Curtis, etc.).\nMathematical Method: Fast Spherical Harmonic Transform via FastSphericalHarmonics.jl.\nComplexity: O(L^3) or O(L^2 log L), where L is the maximum degree (lmax).\nDependencies: Requires using FastSphericalHarmonics.","title":"FSHTSpectralBackend"},{"category":"section","location":"backends/#NUFSHTSpectralBackend","page":"Backends & Extensions","text":"Use Case: Unstructured grids on the sphere (e.g., geodesic grids, scattered ocean stations, planetary orbit tracking).\nMathematical Method: Non-Uniform Fast Spherical Harmonic Transform via NUFSHT.jl.\nComplexity: O(M log M + N log(1epsilon)) (where M is number of modes, N is number of points).\nDependencies: Requires using NUFSHT.\nNote on Coefficient Recovery: Because scattered points are unstructured, the direct SHT projection (adjoint) is not the exact inverse. The backend supports an iterative Conjugate Gradient solver via solve=true to accurately reconstruct coefficients from scattered data.\n\n","title":"NUFSHTSpectralBackend"},{"category":"section","location":"backends/#Execution-backends","page":"Backends & Extensions","text":"The execution axis chooses where and how the chosen transform runs; it is independent of the transform axis. Defaults to AutoBackend().\n\nExecution backend Required library Notes\nSerialBackend() none Single-threaded CPU (always available).\nThreadedBackend() using OhMyThreads Multithreaded CPU: parallelises the direct-sum loop; sets the internal thread count of the FFTW (FFTSpectralBackend) and FINUFFT (FINUFFTBackend) plans (NonuniformFFTsBackend manages its own internal threading). FSHTSpectralBackend/NUFSHTSpectralBackend have no distinct threaded path (execution is a documented no-op there).\nGPUBackend(dev) using KernelAbstractions (+ vendor pkg) GPU execution on the KernelAbstractions device dev (see the GPU table below).\nDistributedBackend(inner) using Distributed Splits work across worker processes, each running inner locally. Parametric, e.g. DistributedBackend(ThreadedBackend()). Requires addprocs + @everywhere using FlowFieldSpectra.\nMPIBackend(inner) using MPI Splits work across MPI ranks, each running inner locally; partials combined with MPI.Allreduce!. MPIBackend(GPUBackend(dev)) targets a multi-GPU cluster. Launch under mpiexec.\nAutoBackend() — Picks ThreadedBackend when OhMyThreads is loaded and Threads.nthreads() > 1, else SerialBackend. Never auto-selects GPU/Distributed/MPI (those need explicit context).\n\nPoint-partitionable transforms (DirectSumSpectralBackend, the NUFFT backends, and NUFSHTSpectralBackend in projection mode) distribute over the point/sample axis and sum the complex coefficients; FFTSpectralBackend/FSHTSpectralBackend (which need the full grid on one process) distribute over the batch/field axis instead.\n\nThe execution axis is result-invariant: it changes only where/how a transform runs, never the answer. One FFT transform under SerialBackend, ThreadedBackend, and GPUBackend(KA.CPU()) returns identical coefficients (to machine ε):\n\n(Image: Execution-axis invariance)","title":"Execution backends"},{"category":"section","location":"backends/#GPU-transforms-on-any-KernelAbstractions-device","page":"Backends & Extensions","text":"GPUBackend{B} is parametric on the KernelAbstractions device object B. Data is staged to that device (KA.allocate + copyto!), and the transform runs there:\n\nFFTSpectralBackend is device-generic through AbstractFFTs.plan_fft, which dispatches on the device array type: CUFFT on a CUDABackend (CuArray), rocFFT on a ROCBackend (ROCArray), and FFTW on KA.CPU() (plain Array). Requires using KernelAbstractions + the FFT provider for your device (FFTW for CPU arrays, CUDA for CUDA, AMDGPU for ROCm). This is not CUDA-specific.\nDirectSumSpectralBackend uses the portable KernelAbstractions direct-sum kernels on any KA device.\nFINUFFTBackend on a GPU uses cuFINUFFT — CUDA-only (FINUFFT.jl provides no portable GPU NUFFT). On a non-CUDA device it raises.\nNonuniformFFTsBackend on a GPU is device-generic through KernelAbstractions: it threads the execution backend into NonuniformFFTs.PlanNUFFT(…; backend=…) and reconstructs FFS's full centered spectrum with broadcasts / a KA kernel (real-input fast path included), so the same code runs on CUDA, ROCm, and KA.CPU(). This is the portable GPU scattered-Cartesian NUFFT; DirectSumSpectralBackend remains the O(N M) portable fallback.\nSpherical (FSHTSpectralBackend/NUFSHTSpectralBackend/DirectSumSpectralBackend) always uses the KA spherical direct-sum kernel — FastSphericalHarmonics and NUFSHT are CPU-only, so there is no fast GPU spherical-harmonic transform.\n\nTransform Grid GPUBackend(CUDABackend()) GPUBackend(KA.CPU()) / other KA device\nFFTSpectralBackend uniform Cartesian CUFFT (via AbstractFFTs) FFTW on KA.CPU(), rocFFT on ROCm, … (via AbstractFFTs)\nFINUFFTBackend scattered Cartesian cuFINUFFT — using CUDA, FINUFFT errors (cuFINUFFT is CUDA-only)\nNonuniformFFTsBackend scattered Cartesian NonuniformFFTs (device-generic, via CUDA) NonuniformFFTs on any KA device (KA.CPU(), ROCm, …)\nDirectSumSpectralBackend any Cartesian KA direct-sum kernel KA direct-sum kernel\nDirectSumSpectralBackend/FSHTSpectralBackend/NUFSHTSpectralBackend any spherical KA spherical direct-sum kernel KA spherical direct-sum kernel\n\nThe device-generic FFT, direct-sum, and NonuniformFFTs NUFFT paths are exercised on CI via GPUBackend(KA.CPU()). The CUDA-specific paths (CUFFT on CuArray, cuFINUFFT) are validated on real CUDA hardware via the package's gpu/ project — CI has no GPU.","title":"GPU transforms on any KernelAbstractions device"},{"category":"section","location":"examples/horizontal_4d/#Horizontal-spectra-of-a-4D-field-on-a-fixed-grid","page":"4D fixed-grid spectra","text":"The defining workflow: horizontal (x, y) spectra of a field f(x, y, z, t) sampled on a fixed, non-uniform horizontal grid. Because the horizontal points never move, the FINUFFT plan and point-sorting are built once and the whole z × t stack is transformed in a single batched execution — then the plan is reused across the time loop. This is the fast path for per-level / per-time spectra of large geophysical datasets.\n\nusing FlowFieldSpectra: FlowFieldSpectra as FFS\nusing FINUFFT: FINUFFT                    # activates the NUFFT extension\nusing Random: Random\nusing SpectralBackends: SpectralBackends as SB\nusing FlowGeometries: FlowGeometries as FG\nRandom.seed!(42)\n\nL = 2π\nN = 48\nnz, nt = 3, 4\nms = (N, N)\nnpts = N * N\n\n# Fixed non-uniform horizontal locations — an unstructured Cartesian grid.\nxv = rand(npts) .* L\nyv = rand(npts) .* L\nhgrid = FG.Grids.UnstructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), (xv, yv), ones(npts);\n    periodic = (true, true), period = (L, L))\n\n# f(x, y, z, t) as a real (Npts, nz, nt) tensor — the (z, t) batch is the trailing axes.\nf = Array{Float64}(undef, npts, nz, nt)\nkz = range(2, 6; length = nz)             # dominant horizontal wavenumber per level\nts = range(0, 1; length = nt)\nfor (it, t) in enumerate(ts), (iz, k0) in enumerate(kz)\n    @. f[:, iz, it] = cos(k0 * xv + 2π * t) + 0.5 * sin((k0 + 1) * yv)\nend\n\n# ONE plan build for the fixed points; transform the whole (z, t) batch in a single exec.\nplan = FFS.plan_spectrum(hgrid, Float64, ms; transform = FFS.FINUFFTBackend(), batch = (nz, nt), eps = 1e-9)\ncoeffs = zeros(ComplexF64, ms..., nz, nt)     # (N, N, nz, nt)\nks = FFS.calculate_spectrum!(coeffs, plan, f)\n\n# ONE batch-preserving reduction → E(k, z, t); average over time → E(k, z).\nnbins = 18\nkbins, E = FFS.isotropic_spectrum(ks, coeffs; num_bins = nbins)   # (nbins, nz, nt)\nEkz = dropdims(sum(E; dims = 3); dims = 3) ./ nt                  # (nbins, nz)","title":"Horizontal spectra of a 4D field on a fixed grid"},{"category":"section","location":"#FlowFieldSpectra.jl","page":"Home","text":"FlowFieldSpectra.jl provides a unified, performance-oriented Julia interface for computing spectral coefficients and spatial energy reductions from flow fields across uniform or unstructured multi-dimensional grids.\n\n","title":"FlowFieldSpectra.jl"},{"category":"section","location":"#Installation","page":"Home","text":"Install the package via Julia's package manager:\n\nusing Pkg\nPkg.add(\"FlowFieldSpectra\")\n\n","title":"Installation"},{"category":"section","location":"#Unified-Workflow-Guide","page":"Home","text":"A typical flow field spectral analysis consists of three main steps:\n\nCoordinate and Field Setup: Define your grid coordinates (zonal, meridional, vertical, or spherical latitude/longitude) and spatial velocity field values.\nSpectral Transform: Run calculate_spectrum with a chosen backend (e.g. DirectSumSpectralBackend, FFTSpectralBackend, NUFFTSpectralBackend, etc.).\nSpectral Reductions: Convert high-dimensional Fourier coefficients to meaningful energy spectra (e.g., 1D isotropic / radial energy density, 1D slice/transect, or spherical degree energy spectra).","title":"Unified Workflow Guide"},{"category":"section","location":"#Quickstart-Tutorial:-Cartesian-2D-Flow-Field","page":"Home","text":"A complete example computing the isotropic energy spectrum of a 2D uniform flow field. You construct an explicit grid (the coordinate system is the grid type — there is no guessing) and pass it to calculate_spectrum.\n\nusing FlowFieldSpectra: FlowFieldSpectra as FFS\nusing FFTW: FFTW                     # activates the FFT extension\nusing SpectralBackends: SpectralBackends as SB\nusing FlowGeometries: FlowGeometries as FG\n\n# 1. Axes of a uniform, periodic Cartesian grid\nL = 2π\nN = 32\nxs = range(0.0, L; length = N + 1)[1:N]\n\n# 2. Zonal/meridional velocities as (N, N) field tensors\nu = [cos(2x) + 0.5 * sin(5y) for x in xs, y in xs]\nv = [sin(2x) for x in xs, y in xs]\n\n# 3. Build the grid (FlowGeometries) and compute Fourier coefficients (the FFT backend needs FFTW)\ngrid = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), xs, xs;\n    periodic = (true, true), period = (L, L))\ncoeffs, ks = FFS.calculate_spectrum(grid, (u, v), (N, N); transform = SB.FFTSpectralBackend())\n\n# 4. Radially integrate to a 1D isotropic (kinetic) energy spectrum (fold the component axis)\nk_bins, E_k = FFS.isotropic_spectrum(ks, coeffs; num_bins = 16, dims = 3)\n\n(Image: Isotropic energy spectrum of a 2D flow field)","title":"Quickstart Tutorial: Cartesian 2D Flow Field"},{"category":"section","location":"examples/cross_spectra/#Cross-spectra-and-flux-by-scale","page":"Cross-spectra & coherence","text":"The co-spectrum Co_{fg}(k) = Re S_{fg}(k) distributes a covariance such as the momentum flux ⟨u'w'⟩ across scales — a staple of boundary-layer and turbulence analysis. Here two correlated fields share a common large-scale mode plus independent small-scale structure.\n\nusing FlowFieldSpectra: FlowFieldSpectra as FFS\nusing FFTW: FFTW                          # activates the FFT extension\nusing SpectralBackends: SpectralBackends as SB\nusing FlowGeometries: FlowGeometries as FG\n\nL = 2π\nN = 64\nxs = range(0.0, L; length = N + 1)[1:N]\n\nu = [cos(2x) + 0.5 * sin(6y) for x in xs, y in xs]     # shared mode at k≈2, own structure at k≈6\nw = [cos(2x) - 0.4 * cos(9x) for x in xs, y in xs]     # shared mode at k≈2, own structure at k≈9\n\ngrid = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), xs, xs;\n    periodic = (true, true), period = (L, L))\ncu, ks = FFS.calculate_spectrum(grid, u, (N, N); transform = SB.FFTSpectralBackend())\ncw, _ = FFS.calculate_spectrum(grid, w, (N, N); transform = SB.FFTSpectralBackend())\n\nk, Co = FFS.cospectrum(ks, cu, cw; num_bins = 24)\n\nThe co-spectrum peaks at the shared scale (k ≈ 2) and is near zero where the two fields have independent structure.","title":"Cross-spectra & flux by scale"},{"category":"section","location":"examples/cross_spectra/#Welch-averaging,-coherence-and-phase","page":"Cross-spectra & coherence","text":"A single realization gives a noisy estimate and no meaningful coherence. Averaging over an ensemble of realizations (the trailing axis of the coefficient array) reduces variance and lets us estimate the magnitude-squared coherence γ²(k) ∈ [0, 1] and the phase between the two fields.\n\nTo recover a meaningful phase we use complex (rotary) signals — e.g. a horizontal velocity u + iv. A complex field has spectral content at +k only, so the cross-spectrum phase survives the radial binning; for a pair of real fields the ±k modes are complex conjugates and the binned phase cancels to zero. Each realization here shares a rotating mode at k ≈ 2 with a fixed phase lead ϕ, plus independent structure elsewhere.\n\nusing Random: Random\nRandom.seed!(1)\n\nnreal = 32\nϕ = 0.7                                        # fixed phase lead of g over f at the shared mode\nCf = zeros(ComplexF64, N, N, nreal)\nCg = zeros(ComplexF64, N, N, nreal)\nfor r in 1:nreal\n    a = 1.0 + 0.1 * randn()                    # shared-mode amplitude jitter\n    fr = [a * exp(im * 2x) + 0.5 * exp(im * (5x) + im * 2π * rand()) for x in xs, y in xs]\n    gr = [a * exp(im * (2x - ϕ)) + 0.5 * exp(im * (7y) + im * 2π * rand()) for x in xs, y in xs]\n    cfr, _ = FFS.calculate_spectrum(grid, fr, (N, N); transform = SB.FFTSpectralBackend())\n    cgr, _ = FFS.calculate_spectrum(grid, gr, (N, N); transform = SB.FFTSpectralBackend())\n    Cf[:, :, r] .= cfr\n    Cg[:, :, r] .= cgr\nend\n\nkw, Ef = FFS.welch_power_spectrum(ks, Cf; num_bins = 24)\nkc, γ², phase = FFS.coherence_spectrum(ks, Cf, Cg; num_bins = 24)\n# Phase is only meaningful where coherence is appreciable; mask the rest.\nphase_plot = [γ²[i] > 0.3 ? phase[i] / π : NaN for i in eachindex(phase)]\n\n(Image: Welch power spectrum alongside coherence² and phase between the two fields)\n\nCoherence is high only at the shared scale k ≈ 2, where the recovered phase matches the imposed lead ϕ (dashed line); elsewhere the independent structure drives coherence toward zero (and the phase, masked here, is meaningless).","title":"Welch averaging, coherence & phase"}]
}
